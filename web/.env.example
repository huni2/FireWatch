VITE_API_BASE_URL=http://localhost:8080
# Design §7 / ADR 0004 — 진짜 비밀이 아니다(클라이언트 번들에 노출됨). 백엔드 SETTINGS_API_KEY와 동일한 값.
VITE_SETTINGS_API_KEY=
# 웹 푸시 VAPID 공개키 — 설계상 공개돼도 되는 값(개인키만 비밀). 백엔드 VAPID_PUBLIC_KEY와 동일한 값.
VITE_VAPID_PUBLIC_KEY=
